{
  "name": "money-mentor-mcp-worker",
  "version": "2.0.0",
  "description": "Money-Mentor MCP Worker — 18 Financial Tools on Cloudflare Edge",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev --config wrangler.toml",
    "deploy": "wrangler deploy --config wrangler.toml"
  },
  "dependencies": {
    "mongodb": "^7.1.1"
  }
}
